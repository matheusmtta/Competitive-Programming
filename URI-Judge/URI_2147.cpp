#include <bits/stdc++.h>

using namespace std;

int mtx[13];

int main(){
    int n;
    cin >> n;
    while(n--){
      string str;
      cin >> str;
      printf("%.2f\n", str.length()/100.0);
    }
    return 0;
}
