#include <bits/stdc++.h>

using namespace std;

int main(){
    long long int n, m;
    while(1){
      cin >> n >> m;
      long long int x, k;
      if (n==0 && m ==0) return 0;
      cout << n*m << endl;
    }
    return 0;
}
